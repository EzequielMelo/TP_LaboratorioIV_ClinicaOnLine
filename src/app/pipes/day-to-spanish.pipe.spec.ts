import { DayToSpanishPipe } from './day-to-spanish.pipe';

describe('DayToSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new DayToSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
